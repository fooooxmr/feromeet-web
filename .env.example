# The production web build points this to the deployed allowlisted proxy.
# Local native builds may use https://feromeet.com directly.
EXPO_PUBLIC_API_URL=https://proxy-snowy-six-76.vercel.app/api/proxy

# Enables safe sample data when no test session is available.
EXPO_PUBLIC_DEMO_MODE=true
